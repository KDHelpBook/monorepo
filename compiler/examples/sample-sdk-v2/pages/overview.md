---
title: Overview
keywords: [sample sdk, overview]
categories: [guide]
related: [migration]
---
# Sample SDK

You are reading the **2.0** documentation — the latest version, shown by default.
Pick **1.0** from the Version selector to see the older docs.

## What's new in 2.0

- `sendAsync()` returns a promise that resolves on delivery acknowledgement.
- `send()` is **deprecated** — see [Migrating from 1.0](migration).
