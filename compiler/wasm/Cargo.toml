[package]
name = "khb-wasm"
description = "Internal placeholder for possible future WebAssembly integration"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[lib]
# Keep an rlib so the workspace can build and test the placeholder natively.
crate-type = ["cdylib", "rlib"]

[dependencies]
khb-core = { path = "../core" }
